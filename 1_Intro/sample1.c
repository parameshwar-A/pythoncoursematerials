#include <stdio.h>
int main() {    

    inte number1, number2, sum;//error 1 
    
    printf("Enter two integers: ");
    scanf("%d %d", &number1, &number2);

    // calculating sum
    sum = number1 + number2;      
    
    printd("%d + %d = %d", number1, number2, sum);//error2
    return 0;
}

